jQuery(document).ready(function($) {
    $(".related-posts-section .items").flickity({
        // options
        cellAlign: "left",
        groupCells: true,
        pageDots: false,
        contain: true
    });
});
